<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--매칭 종류 선택 Fragment-->

    <!--타이틀-->
    <TextView
        android:id="@+id/match_home_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:text="매칭 선택"
        android:textSize="50sp"
        android:layout_marginTop="20sp"
        />

    <!--일반매칭 버튼-->
    <Button
        android:id="@+id/match_btn1"
        android:backgroundTint="@color/teal_200"
        android:background="@drawable/round"
        android:layout_width="300sp"
        android:layout_height="wrap_content"
        android:text="일반 매칭"
        android:textSize="25sp"
        android:padding="10sp"
        android:textColor="@color/white"
        android:drawableBottom="@drawable/match_btn1"
        android:drawablePadding="10sp"
        app:layout_constraintTop_toBottomOf="@id/match_home_title"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="20sp"
        />

    <!--강사 매칭 버튼-->
    <Button
        android:id="@+id/match_btn2"
        android:backgroundTint="@color/teal_200"
        android:background="@drawable/round"
        android:layout_width="300sp"
        android:layout_height="wrap_content"
        android:text="강사 매칭"
        android:textSize="25sp"
        android:padding="10sp"
        android:textColor="@color/white"
        android:drawableBottom="@drawable/match_btn2"
        android:drawablePadding="10sp"
        app:layout_constraintTop_toBottomOf="@id/match_btn1"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="20sp"
        />


</androidx.constraintlayout.widget.ConstraintLayout>